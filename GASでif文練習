} else if (bzColumnValue === "撤去可能ビル") {
  category = 'possibleRemoval';
  rawDate = row[colIndexes.acColumn];
  console.log(`撤去可能ビル rawDate: ${rawDate}, 型: ${typeof rawDate}`);
  if (rawDate instanceof Date) {
    console.log('Date型です');
  } else if (typeof rawDate === 'string') {
    console.log('文字列型です');
  } else if (rawDate === null) {
    console.log('null値です');
  } else if (rawDate === undefined) {
    console.log('undefined値です');
  } else {
    console.log(`その他の型です: ${Object.prototype.toString.call(rawDate)}`);
  }
}
